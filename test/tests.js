const assert = require('assert')
const payable = require('../index')
const { rejects } = require('assert')

describe("PAYable SDK", function () {

	it("validation test", async function () {
		assert.equal(0, 0)
	})

})
